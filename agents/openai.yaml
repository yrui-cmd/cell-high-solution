interface:
  display_name: "cell_high_solution"
  short_description: "Upscale or text-guide one blurry image into a sharp HD PNG"
  default_prompt: "Use $cell-high-solution to enhance this blurry target image, using a UTF-8 visual-guidance file only if I explicitly request semantic redraw."
